:80

# Serve frontend build
root * /frontend/dist
file_server

# SPA routing
try_files {path} /index.html

# Proxy API requests (keep /api prefix)
handle /api/* {
    reverse_proxy http://backend:5000
}

# Proxy WebSocket requests
handle /ws/* {
    reverse_proxy http://backend:5000
}
