:80

# Serve Vite build
root * /srv/dist
file_server

# SPA routing
try_files {path} /index.html

# Optional: API reverse proxy if backend is running
handle_path /api/* {
    reverse_proxy http://localhost:5000
}
